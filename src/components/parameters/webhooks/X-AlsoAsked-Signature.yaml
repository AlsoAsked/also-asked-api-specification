name: X-AlsoAsked-Signature
in: header
description: |
  The signature of the webhook request. The signature is generated using the HMAC-SHA256 algorithm and the
  webhook secret as the key. The signature is in the hex format. You should use this signature to verify that the
  webhook request originated from AlsoAsked.
required: true
schema:
  type: string
  pattern: ^[a-fA-F0-9]{64}$
  minLength: 64
  maxLength: 64
example: a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0c1d2e3f4a5b6c7d8e9f05a7c
