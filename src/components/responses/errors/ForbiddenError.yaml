description: Forbidden
content:
  application/json:
    schema:
      oneOf:
        - $ref: ./InvalidApiKeyError.yaml
        - $ref: ./ApiAccessDisabledError.yaml
      discriminator:
        propertyName: status
